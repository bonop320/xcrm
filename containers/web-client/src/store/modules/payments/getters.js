// import {
//   map,
//   assoc,
//   find,
//   propEq,
//   filter
// } from 'ramda'

const all = (state) => {
  return state.raw
  // const tagAgent = x => {
  //   const of = rootGetters['users/byId']
  //   return assoc('agent', of(x.book), x)
  // }
  //
  // return map(tagAgent, state.raw)
}

export {
  all
}
